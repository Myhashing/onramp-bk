spring:
  application:
    name: onramp
  datasource:
    url: jdbc:mysql://localhost:3306/onramper
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          fusionauth:
            client-id: f7427948-34ae-4dc0-920d-f50316d67d70
            client-secret: RLN9NlryIkmXw6_BNbsxs-95sOlHpRV7nG11M0jgonI
            scope: openid, profile, email
            redirect-uri: http://localhost:8080/auth/callback
            authorization-grant-type: authorization_code
            client-authentication-method: post
        provider:
          fusionauth:
            authorization-uri: http://localhost:9011/oauth2/authorize
            token-uri: http://localhost:9011/oauth2/token
            user-info-uri: http://localhost:9011/oauth2/userinfo
            jwk-set-uri: http://localhost:9011/.well-known/jwks.json
            user-name-attribute: sub